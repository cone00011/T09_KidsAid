 <section class="sticky-photo">
 <div class="slideshow-container">
            <div class="mySlides fade">
                <img src="../../public/img/tryllekunstner_ballon.webp" style="width:100%" alt="tryllekunstner_ballon">
            </div>

            <div class="mySlides fade">
                <img src="../../public/img/tryllekunster_kanin.webp" style="width:100%" alt="tryllekunstner_kanin">
            </div>

            <div class="mySlides fade">
                <img src="../../public/img/tryllekunstner_hospitalsseng.webp" style="width:100%" alt="tryllekunstner_hospitalsseng">
            </div>
            <img class="pink_hund" src="../../public/elementer/pink_hund.svg" alt="pink_hund">
            <img class="gul_hund" src="../../public/elementer/gul_hund.svg" alt="gul_hund">
        </div>
        </section>

        <script>
var slideIndex = 0; // Startindex for billedkarrusellen

// Funktionen til at vise næste billede
function showNextSlide() {
  var slides = document.querySelectorAll(".mySlides");

  for (var i = 0; i < slides.length; i++) {
    slides[i].style.display = "none"; // Skjul alle billeder
  }

  slideIndex++; // Øg slide-indekset
  if (slideIndex > slides.length) {
    slideIndex = 1; // Start forfra, når vi når slutningen
  }

  slides[slideIndex - 1].style.display = "block"; // Vis det aktuelle billede
  setTimeout(showNextSlide, 2200); // Skift til næste billede efter 9 sekunder
}

// Start billedkarrusellen
showNextSlide();

function toggleMenu() {
  const menu = document.querySelector(".menu");
  menu.classList.toggle("active");
}
        </script>

<style>
html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  scroll-behavior: smooth;
}

.mySlides {
  display: none; /* Skjuler billederne som standard */
}

.mySlides img {
  width: 100%; /* Gør billedet så bredt som containeren */
  height: 80vh; /* Sætter højden til 80% af viewporten */
  object-fit: cover; /* Bevarer billedets proportioner, mens det fylder containeren */
  position: sticky;
}

.slideshow-container {
  width: 100%;
  height: 100%;
  position: relative;
  padding-top: 100px;
  padding-bottom: 50px;
}

.pink_hund {
  position: absolute;
  top: 0;
  left: 0;
  width: 250px; /* Juster størrelsen som ønsket */
  z-index: 10; /* Sørger for, at hunden ligger oven på billedet */
  padding-left: 20px;
}

.gul_hund {
  position: absolute;
  bottom: 0;
  right: 0;
  width: 250px; /* Juster størrelsen som ønsket */
  z-index: 10; /* Sørger for, at hunden ligger oven på billedet */
  padding-right: 20px;
}


body {
      margin: 0;
      font-family: Arial, sans-serif;
      scroll-behavior: smooth;
    }
  
  
    .sticky-photo {
      position: sticky;
      top: 0;
      width: 100vw; /* Fylder hele bredden */
      height: 100vh; /* Fylder hele højden */
      object-fit: cover; /* Dækker skærmen */
    }

</style>

